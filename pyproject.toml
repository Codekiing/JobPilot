[project]
name = "jobpilot-workbench"
version = "0.1.0"
description = "Unified local runtime for the JobPilot API and its components"
requires-python = ">=3.10"
dependencies = [
  "pdfplumber>=0.11,<1",
  "playwright>=1.50,<2",
  "python-docx>=1.1,<2",
]

[tool.uv]
package = false
